# uv config for restaurant service
uvicorn:
  app: app.main:app
  host: 0.0.0.0
  port: 8001
  reload: true 
processes:
  - name: api
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
  - name: batch
    command: python -m batch.batch_jobs 